---
id: S-238
type: Story
title: 'D4: skill-driven E2E harness via claude -p — outcome assertions over setup/add/close-iteration'
status: Done
parent: F-123
js: 8
bv: 0
tc: 0
rr_oe: 0
wsjf: 0.0
closed_at: '2026-06-10T21:57:55Z'
---

Fills Layer 3 of the `docs/E2E-SKILLS-TEST-PLAN.md` Příloha D strategy pyramid (currently ❌ open): skill **side-effect** testing via `claude -p` subprocess + **outcome assertions** (filesystem state + git audit trail), not transcript matching (LLM nondeterminism).

Harness `tests/test_skill_e2e.py`:
- Drives the **working-tree** plugin via `--plugin-dir <repo>/plugin` (not the stale installed cache), `--permission-mode bypassPermissions`, `--output-format text`.
- `_claude()` helper = `subprocess.run([...], timeout=)` (no GNU `timeout` binary — not on macOS).
- Session-scoped sandbox fixture: fresh temp git repo, runs `/edpa:setup` once.
- Covers 3 skill flows: **setup** (engine vendored + 4 configs), **add** (ID alloc from id_counters.yaml + `feat(S-N):` auto-commit), **close-iteration** (engine runs + `Σ hours == capacity` invariant).
- Opt-in: `@pytest.mark.skill_e2e` + auto-skip unless `EDPA_SKILL_E2E=1` and `claude` on PATH — keeps default `pytest -m "not e2e"` and CI green + cost-free (no API key needed in CI).

Feasibility verified 2026-06-10: `claude -p "/edpa:setup" --plugin-dir … --permission-mode bypassPermissions` → exit 0, engine vendored (48 scripts, VERSION 2.6.0), configs seeded.

Supersedes the partly-V1-stale spec in TODO.md §"Skill side-effect testing" (drops obsolete issue_map.yaml / sync-push / gh-project assertions removed in 2.0.0).
