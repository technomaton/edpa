---
id: D-21
type: Defect
title: pi_planning/reconcile/transitions crash on Windows cp1252 console (UnicodeEncodeError on glyph print) — add 
  _console guard
status: Funnel
iteration: PI-2026-2.2
assignee: urbanek
js: 2
bv: 0
tc: 0
rr_oe: 0
wsjf: 0.0
evidence:
- type: commit_author
  person: urbanek
  weight: 2.78
  ref: commit/83616e0
  at: '2026-06-17T10:00:50+02:00'
- type: agent_contribution
  agent: claude-opus-4-8-(1m-context)
  person: _claude
  weight: 1.0
  ref: commit/83616e0/agent/claude-opus-4-8-(1m-context)
  at: '2026-06-17T10:00:50+02:00'
- type: commit_author
  person: urbanek
  weight: 2.78
  ref: commit/efd1e50
  at: '2026-06-17T10:04:18+02:00'
---

Windows regression. `_console` (2.1.9) reconfigures stdout to UTF-8 so EDPA's progress glyphs don't crash a cp1250/cp1252 console, but three entry points shipped WITHOUT the guarded `import _console`:

- `pi_planning.py` L331 — `print("✓ …")`. Reported via `/edpa:pi-planning` on Windows: the run wrote the full ~787 kB report, then exited 1 on `UnicodeEncodeError '✓'` in cp1252.py.
- `reconcile.py` L263 — `print("… — …")` (em-dash).
- `transitions.py` L317 — `print("∅")`.

Fix: add the lazy, ImportError-guarded `import _console` at the top of each `main()`. Lazy (inside main, not module-top) because `pi_planning`/`reconcile` are imported by `mcp_server` and must keep JSON-RPC stdout pristine — mirrors `create_pi.py`.

Secondary: `pi_planning.py --open` never opened the browser on Windows — `subprocess.run(["start", path])` fails because `start` is a cmd builtin, not an executable (FileNotFoundError, swallowed by the bare except). Switched to `os.startfile` on Windows / `open` (mac) / `xdg-open` (linux).

Regression guard: new `tests/test_console.py::test_glyph_printing_entry_points_import_console` AST-walks every entry point and fails if one `print()`s a non-ASCII literal without importing `_console` (mcp_server exempt — its glyphs live in JSON-RPC descriptions, framed UTF-8 by the SDK). `test_encoding_hygiene` only guarded file I/O encoding, not stdout — that's the gap that let this through.

Verified: reproduced the exact `UnicodeEncodeError '✓'`, confirmed fixed; ran all three scripts end-to-end under `PYTHONIOENCODING=cp1252` (exit 0, HTML written); 110 tests pass.
