---
import Layout from '../layouts/Layout.astro';
import { VERSION } from '../lib/version';
---
<Layout title={`Metodika EDPA v${VERSION}`} description={`Kompletní metodika EDPA v${VERSION} — evidence-driven proportional allocation, 13 sekcí od modelu po implementaci.`}>
<div class="container section methodology-page">

  <div class="page-header">
    <div class="tag">Metodika</div>
    <h1>EDPA — Evidence-Driven Proportional Allocation</h1>
    <p class="subtitle">Odvozování kapacity z dodávkové evidence</p>
    <p class="meta">Verze {VERSION} &middot; Květen 2026 &middot; Jaroslav Urbanek, Lead Architect</p>
  </div>

  <div class="methodology-layout">

  <aside class="sidebar-toc" id="sidebarToc">
    <div class="toc-title">Obsah</div>
    <ol class="toc-list">
      <li><a href="#shrnuti" class="toc-link" data-section="shrnuti">Shrnutí</a></li>
      <li><a href="#terminologie" class="toc-link" data-section="terminologie">Terminologie</a></li>
      <li><a href="#architektura" class="toc-link" data-section="architektura">Architektura modelu</a></li>
      <li><a href="#hierarchie" class="toc-link" data-section="hierarchie">Hierarchie work items</a></li>
      <li><a href="#model" class="toc-link" data-section="model">Model EDPA</a></li>
      <li><a href="#dual-view" class="toc-link" data-section="dual-view">Dual-view</a></li>
      <li><a href="#kadence" class="toc-link" data-section="kadence">Kadence</a></li>
      <li><a href="#ucici-smycka" class="toc-link" data-section="ucici-smycka">Učící smyčka</a></li>
      <li><a href="#github-implementace" class="toc-link" data-section="github-implementace">GitHub implementace</a></li>
      <li><a href="#vykazy" class="toc-link" data-section="vykazy">Výkazy a audit</a></li>
      <li><a href="#predpoklady" class="toc-link" data-section="predpoklady">Předpoklady a rizika</a></li>
      <li><a href="#srovnani" class="toc-link" data-section="srovnani">Srovnání</a></li>
      <li><a href="#implementacni-plan" class="toc-link" data-section="implementacni-plan">Implementační plán</a></li>
    </ol>
  </aside>

  <div class="methodology-content">

  <!-- 1. Shrnutí -->
  <section id="shrnuti">
    <h2>1. Shrnutí</h2>
    <div class="quote">Čas se neměří, odvozuje se. Nikdo neloguje hodiny. Nikdo nevyplňuje timesheets.</div>
    <p>Člověk deklaruje kapacitu na období. Systém identifikuje work items, na nichž se prokazatelně podílel. Kapacita se ex post rozpadá poměrově mezi relevantní items podle Job Size a míry contribution.</p>
    <p>Výsledkem je výkaz, který je vedlejším produktem dodávky, ne separátní administrativní aktivitou.</p>
    <p>Model poskytuje dva komplementární pohledy na stejná data:</p>
    <ul>
      <li><strong>Per-person pohled:</strong> jak se kapacita člověka rozloží mezi jeho items &rarr; výkazy, timesheets</li>
      <li><strong>Per-item pohled:</strong> jak se práce na itemu rozloží mezi lidi &rarr; nákladová alokace, audit per deliverable</li>
    </ul>
  </section>

  <!-- 2. Terminologie -->
  <section id="terminologie">
    <h2>2. Terminologie</h2>
    <div class="card">
      <table>
        <thead>
          <tr><th>Pojem</th><th>Definice</th><th>Konfigurace</th></tr>
        </thead>
        <tbody>
          <tr><td><strong>Iterace</strong></td><td>Delivery cyklus. Stories se plánují, dodávají a uzavírají.</td><td>1t (AI-native) / 2t (klasicky)</td></tr>
          <tr><td><strong>Planning Interval</strong></td><td>Plánovací cyklus. Features se plánují, koordinují a vyhodnocují.</td><td>5t (4+1 IP) / 10t (8+2 IP)</td></tr>
          <tr><td><strong>IP Iterace</strong></td><td>Innovation &amp; Planning iterace na konci PI.</td><td>Poslední iterace v PI</td></tr>
          <tr><td><strong>Job Size (JS)</strong></td><td>Relativní odhad velikosti work itemu. Upravená Fibonacci.</td><td>1, 2, 3, 5, 8, 13, 20</td></tr>
          <tr><td><strong>WSJF</strong></td><td>Prioritizační skóre: (BV + TC + RR) / JS</td><td>Per úroveň nezávisle</td></tr>
          <tr><td><strong>CW</strong></td><td>Contribution Weight — míra zapojení osoby na itemu. Nezávisle per osoba.</td><td>0.15&ndash;1.0</td></tr>
          <tr><td><strong>Evidence Score</strong></td><td>Surový součet signálů aktivity z GitHub. Detekční vrstva.</td><td>Automaticky</td></tr>
          <tr><td><strong>RS</strong></td><td>Relevance Signal — normalizovaný signál odvozený z Evidence Score.</td><td>Automaticky</td></tr>
          <tr><td><strong>Derived Hours</strong></td><td>Odvozené hodiny, výstup modelu.</td><td>Po Iteration Close</td></tr>
        </tbody>
      </table>
      <div class="note">Evidence Score je detekční vrstva &rarr; Relevance Signal je matematický vstup &rarr; Contribution Weight je podíl &rarr; Derived Hours jsou výstup.</div>
    </div>
  </section>

  <!-- 3. Architektura modelu -->
  <section id="architektura">
    <h2>3. Architektura modelu</h2>
    <h3>3.1 Tři oddělené vrstvy</h3>
    <div class="card">
      <table>
        <thead>
          <tr><th>Vrstva</th><th>Účel</th><th>Kde žije</th></tr>
        </thead>
        <tbody>
          <tr><td class="layer-operational"><strong>Operational Metadata</strong></td><td>Živá delivery data</td><td>GitHub Issues + Projects</td></tr>
          <tr><td class="layer-capacity"><strong>Capacity Registry</strong></td><td>Kapacita lidi, role, FTE</td><td>YAML / JSON v repo</td></tr>
          <tr><td class="layer-evidence"><strong>Evidence &amp; Reporting</strong></td><td>Frozen snapshoty, výkazy, podpisy</td><td>/snapshots, /reports, /signed</td></tr>
        </tbody>
      </table>
    </div>

    <h3>3.2 Source of truth</h3>
    <p><strong>GitHub JE source of truth pro:</strong> issue hierarchy, ownership, status, Job Size, WSJF inputs, review trail, delivery audit trail.</p>
    <p><strong>GitHub NENÍ source of truth pro:</strong> hodinovou kapacitu, FTE, derived hours, podpisové stavy. Tyto žijí v evidence vrstvě.</p>
  </section>

  <!-- 4. Hierarchie work items -->
  <section id="hierarchie">
    <h2>4. Hierarchie work items</h2>
    <div class="formula" style="white-space:pre;line-height:2;font-size:16px"><span class="wi-initiative"><b>Initiative</b></span> (celý projekt, business case)
  └── <span class="wi-epic"><b>Epic</b></span> (strategický cíl, 6-9 měsíců)
       └── <span class="wi-feature"><b>Feature</b></span> (musí se vejít do Planning Intervalu)
            └── <span class="wi-story"><b>Story</b></span> (dodáváno v Iteraci)
                 └── <span class="wi-task"><b>Task</b></span> (technická práce, volitelné)</div>
    <div class="legend">
      <div class="legend-item"><div class="legend-dot" style="background:var(--pk)"></div> Initiative</div>
      <div class="legend-item"><div class="legend-dot" style="background:var(--ac2)"></div> Epic</div>
      <div class="legend-item"><div class="legend-dot" style="background:var(--cy)"></div> Feature</div>
      <div class="legend-item"><div class="legend-dot" style="background:var(--or)"></div> Story</div>
      <div class="legend-item"><div class="legend-dot" style="background:var(--yl)"></div> Task</div>
    </div>
    <p>Každá úroveň má <strong>vlastní nezávislý Job Size a WSJF</strong>. Feature WSJF se nepočítá ze Stories pod ní.</p>

    <div class="card" style="margin-top:16px">
      <table>
        <thead>
          <tr><th>Úroveň</th><th>JS max</th><th>GitHub mapping</th></tr>
        </thead>
        <tbody>
          <tr><td class="wi-epic"><b>Epic</b></td><td>20</td><td>Issue Type: Epic</td></tr>
          <tr><td class="wi-feature"><b>Feature</b></td><td>13</td><td>Issue Type: Feature</td></tr>
          <tr><td class="wi-story"><b>Story</b> (2/10)</td><td>8</td><td>Issue Type: Story</td></tr>
          <tr><td class="wi-story"><b>Story</b> (1/5)</td><td>5</td><td>Issue Type: Story</td></tr>
          <tr><td class="wi-task"><b>Task</b></td><td>&mdash;</td><td>Issue Type: Task</td></tr>
        </tbody>
      </table>
    </div>

    <p style="margin-top:16px">Nad limitem se item rozpadá. Granularity guardrails vynucují rozumnou granularitu.</p>

    <div class="formula"><strong>WSJF:</strong> (<span class="wsjf-bv">BV</span> + <span class="wsjf-tc">TC</span> + <span class="wsjf-rr">RR</span>) / <span class="wsjf-js">JS</span>
<span class="wsjf-bv">BV</span> = Business Value, <span class="wsjf-tc">TC</span> = Time Criticality, <span class="wsjf-rr">RR</span> = Risk Reduction
Počítá se per úroveň nezávisle.</div>
    <div class="legend">
      <div class="legend-item"><div class="legend-dot" style="background:var(--gn)"></div> BV — Business Value</div>
      <div class="legend-item"><div class="legend-dot" style="background:var(--or)"></div> TC — Time Criticality</div>
      <div class="legend-item"><div class="legend-dot" style="background:var(--cy)"></div> RR — Risk Reduction</div>
      <div class="legend-item"><div class="legend-dot" style="background:var(--yl)"></div> JS — Job Size</div>
    </div>
  </section>

  <!-- 5. Model EDPA -->
  <section id="model">
    <h2>5. Model: Evidence-Driven Proportional Allocation</h2>

    <div id="planning-protocol">
    <h3>5.1 Protokol plánování iterace</h3>
    <p>Než EDPA odvodí hodiny (ex-post), tým musí naplánovat iteraci (ex-ante). Plánování vyžaduje potvrzenou kapacitu jako vstup.</p>
    <div class="card">
      <ol>
        <li><strong>Potvrdit kapacitu</strong> &mdash; Každý člen týmu potvrdí dostupnost. Jde o závazek, ne odhad. Externí spolupracovníci vyjednávají alokaci explicitně. Výsledek: <code>availability: confirmed</code> v <code>people.yaml</code>.</li>
        <li><strong>Vypočítat Planning Capacity</strong> &mdash; <code>Planning_Capacity = &Sigma; Capacity[P, I] &times; planning_factor</code>. <code>planning_factor</code> je <strong>vlastnost týmu</strong> (konfigurováno per tým v <code>people.yaml</code> pod <code>teams:</code>). Výchozí: 0.8.</li>
        <li><strong>Vybrat práci</strong> &mdash; Vytáhni stories z prioritizovaného backlogu (WSJF pořadí) dokud <code>&Sigma; JobSize</code> nedosáhne historické velocity &times; <code>planning_factor</code>. Neplánuj na 100%.</li>
        <li><strong>Buffer</strong> &mdash; Zbylých ~20% absorbuje support, maintenance, incidenty a neplánovanou práci. Pokud buffer položky generují evidenci, EDPA je alokuje normálně.</li>
        <li><strong>Edge case</strong> &mdash; Pokud žádná neplánovaná práce nenastane, veškerá kapacita jde na plánované položky. Garance <code>&Sigma; = Capacity</code> platí vždy.</li>
      </ol>
    </div>
    <div class="note" style="margin-top: 12px;"><strong>Proč 80%?</strong> Plánování na 100% vynucuje jeden ze tří failure modes: nedodané stories, přetížení, nebo scope creep. Heuristika 80% je konzistentní se SAFe load factor, Scrum velocity-based planning a Kanban WIP limits. Různé týmy si mohou zvolit různé faktory podle svého support loadu a zralosti.</div>
    </div>

    <h3>5.2 Vstupy</h3>
    <div class="card">
      <table>
        <thead>
          <tr><th>Vstup</th><th>Zdroj</th><th>Příklad</th></tr>
        </thead>
        <tbody>
          <tr><td><code>Capacity[P, I]</code></td><td>Potvrzeno při Iteration Planning</td><td>40h</td></tr>
          <tr><td><code>RelevantItems[P, I]</code></td><td>Automaticky z GitHub evidence</td><td>6 items přes 3 úrovně</td></tr>
          <tr><td><code>JobSize[item]</code></td><td>Custom field na issue</td><td>Fibonacci 1&ndash;20</td></tr>
          <tr><td><code>CW[P, item]</code></td><td>Z evidence / manuální override</td><td>0.15&ndash;1.0</td></tr>
          <tr><td><code>RS[P, item]</code></td><td>Normalizováno z Evidence Score</td><td>0.25&ndash;1.0</td></tr>
        </tbody>
      </table>
    </div>

    <h3>5.3 Evidence detection</h3>
    <div class="card">
      <table>
        <thead>
          <tr><th>GitHub signal</th><th style="text-align:right">Evidence Score</th><th style="text-align:right">Typický CW</th></tr>
        </thead>
        <tbody>
          <tr><td>Assignee na issue</td><td style="text-align:right">+4</td><td style="text-align:right;color:var(--gn)">1.0 (owner)</td></tr>
          <tr><td>Explicitní <code>/contribute</code> příkaz</td><td style="text-align:right">+3</td><td style="text-align:right;color:var(--cy)">explicit</td></tr>
          <tr><td>PR author referencující item</td><td style="text-align:right">+2</td><td style="text-align:right;color:var(--cy)">0.6 (key)</td></tr>
          <tr><td>Commit author s ref v message</td><td style="text-align:right">+1</td><td style="text-align:right;color:var(--yl)">0.25 (reviewer)</td></tr>
          <tr><td>PR reviewer</td><td style="text-align:right">+1</td><td style="text-align:right;color:var(--yl)">0.25 (reviewer)</td></tr>
          <tr><td>Issue / PR comment</td><td style="text-align:right">+0.5</td><td style="text-align:right;color:var(--or)">0.15 (consulted)</td></tr>
        </tbody>
      </table>
    </div>
    <ul>
      <li>Threshold: Evidence Score &ge; 1.0</li>
      <li>Heuristika: nejsilnější signál &rarr; výchozí CW</li>
      <li>Override: <code>/contribute @osoba weight:0.6</code></li>
      <li>Commit count se nepřevádí na čas</li>
    </ul>

    <div class="note" style="margin-top:12px;border-left:3px solid var(--gn);padding-left:12px"><strong>v1.11 změna:</strong> Per-role korekce (<code>role_overrides</code>) byly v v1.11 odstraněny. CW se počítá výhradně signálovou agregací a per-item normalizací (viz § 5.4 níže). Strategická-role bias se nyní řeší kalibrací signálových vah proti ground truth — pokud BO/PM/Arch dostávají málo, kalibrátor zvedne <code>issue_comment</code> a <code>pr_reviewer</code> váhy. Detail v <a href="https://github.com/technomaton/edpa/blob/main/docs/auto-calibration.md" style="color:var(--ac2)">auto-calibration.md</a>.</div>

    <h3>5.4 Výpočet (v1.11) — single source, sum-and-normalize</h3>
    <div class="formula"><strong>contribution_score[P, item]</strong> = &Sigma; signal_weight &times; signal_fired(P, item)
<strong>cw[P, item]</strong> = contribution_score[P, item] / &Sigma;<sub>persons</sub> contribution_score[*, item]
<strong>score[P, item]</strong> = JobSize[item] &times; cw[P, item]
<strong>ratio[P, item]</strong> = score[P, item] / &Sigma;<sub>items_of_P</sub> score
<strong>DerivedHours[P, item]</strong> = ratio[P, item] &times; Capacity[P, I]</div>
    <div class="note"><strong>Klíčová změna oproti v1.10:</strong> <code>cw</code> je teď per-item podíl (Σ napříč osobami = 1.0 na položce), ne absolutní [0,1] role-coupled hodnota. Žádný <code>RelevanceSignal</code>, žádný role priority lookup, žádné dva varianty Score formula. Signály sčítají additivně, normalizují se per-item, engine konzumuje hotové <code>cw</code>.
    <br>Příklad: <a href="/calculator/" style="color:var(--ac2);">Kalkulátor</a>. Plná specifikace: <a href="https://github.com/technomaton/edpa/blob/main/docs/methodology.md#54-calculation--three-modes" style="color:var(--ac2)">methodology.md § 5.4</a>.</div>

    <h3>5.5 Matematické garance (dvě)</h3>
    <div class="formula"><strong>1. Per-item:</strong>   &Sigma;<sub>persons</sub> cw[*, item] = 1.0
<strong>2. Per-person:</strong> &Sigma;<sub>items</sub> DerivedHours[P, *] = Capacity[P, I]

Obě platí strukturálně — z konstrukce sum-and-normalize aggregation
a ratio normalizace per osobu napříč iteračními položkami.
Engine je při běhu validuje a snapshot odmítne zapsat při porušení.</div>
  </section>

  <!-- 6. Dual-view -->
  <section id="dual-view">
    <h2>6. Dual-view CW: dvě otázky, jeden dataset</h2>
    <p>CW = 0.25 pro reviewera může znamenat dvě věci. Jsou to dvě různé otázky — model řeší obě ze stejných dat:</p>

    <div class="grid grid-2" style="margin-top:16px">
      <div class="card green">
        <div class="tag" style="color:var(--gn)">Per-person normalizace</div>
        <p><strong>Otázka:</strong> Jak se kapacita člověka P rozloží mezi jeho items?</p>
        <div class="formula">DerivedHours[P, item] = (Score[P, item] / &Sigma; Score[P, *]) &times; Capacity[P, I]

Garance: &Sigma; DerivedHours[P, *] = Capacity</div>
        <p class="meta">Výstup: výkaz per osoba pro OP TAK</p>
      </div>
      <div class="card cyan">
        <div class="tag" style="color:var(--cy)">Per-item normalizace</div>
        <p><strong>Otázka:</strong> Jak se práce na itemu X rozloží mezi lidi?</p>
        <div class="formula">ItemShare[P, item] = DerivedHours[P, item] / &Sigma; DerivedHours[*, item]

Garance: &Sigma; podilu na itemu = 100 %</div>
        <p class="meta">Výstup: nákladová karta per deliverable</p>
      </div>
    </div>

    <div class="card" style="margin-top:16px">
      <table>
        <thead>
          <tr><th>Pohled</th><th>Otázka</th><th>Výstup</th><th>Garance</th></tr>
        </thead>
        <tbody>
          <tr><td>Per-person</td><td>Kolik hodin P strávil na čem?</td><td>Výkaz, OP TAK</td><td style="color:var(--gn)">&Sigma; = kapacita</td></tr>
          <tr><td>Per-item</td><td>Kolik lidí a hodin stál item X?</td><td>Nákladová alokace</td><td style="color:var(--cy)">&Sigma; podilu = 100%</td></tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- 7. Kadence -->
  <section id="kadence">
    <h2>7. Konfigurace kadence</h2>
    <div class="grid grid-2">
      <div class="card yellow">
        <div class="tag" style="color:var(--yl)">Varianta A: Klasická (2/10)</div>
        <table>
          <thead>
            <tr><th>Cyklus</th><th>Délka</th><th>1.0 FTE</th><th>0.5</th><th>0.25</th></tr>
          </thead>
          <tbody>
            <tr><td>Iterace</td><td>2 týdny</td><td>80h</td><td>40h</td><td>20h</td></tr>
            <tr><td>PI</td><td>10 týdnů</td><td>400h</td><td>200h</td><td>100h</td></tr>
          </tbody>
        </table>
      </div>
      <div class="card green">
        <div class="tag" style="color:var(--gn)">Varianta B: AI-Native (1/5)</div>
        <table>
          <thead>
            <tr><th>Cyklus</th><th>Délka</th><th>1.0 FTE</th><th>0.5</th><th>0.25</th></tr>
          </thead>
          <tbody>
            <tr><td>Iterace</td><td>1 týden</td><td>40h</td><td>20h</td><td>10h</td></tr>
            <tr><td>PI</td><td>5 týdnů</td><td>200h</td><td>100h</td><td>50h</td></tr>
          </tbody>
        </table>
      </div>
    </div>
    <div class="note">Doporučení: začít na A. Po prvním PI vyhodnotit přechod na B na základě velocity a lead time dat.</div>
  </section>

  <!-- 8. Učící smyčka -->
  <section id="ucici-smycka">
    <h2>8. Učící smyčka</h2>
    <div class="grid grid-2">
      <div class="card green">
        <div class="tag" style="color:var(--gn)">Velocity tracking</div>
        <div class="formula">Story_Velocity = &Sigma; JS uzavřených Stories / iterace
Feature_Velocity = &Sigma; JS uzavřených Features / PI
Accuracy = Actual / Planned &times; 100 %</div>
      </div>
      <div class="card accent">
        <div class="tag" style="color:var(--ac3)">Kalibrace</div>
        <ul>
          <li><strong>CW:</strong> Po 2&ndash;3 iteracích vyhodnotit heuristiku. Monte Carlo validace per-role korekcí (viz 5.3.1).</li>
          <li><strong>Job Size:</strong> Referenční Story &ldquo;3&rdquo; &ne; Feature &ldquo;3&rdquo;. Per úroveň nezávisle.</li>
          <li><strong>AI:</strong> Vykazuješ čas na dodání, ne minuty kódu. AI &rarr; velocity, ne výkaz.</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- 9. GitHub implementace -->
  <section id="github-implementace">
    <h2>9. Implementace v GitHub</h2>

    <h3>9.1 Native Issue Types &amp; custom fields</h3>
    <p>Issue Types jsou <strong>nativní funkce GitHubu</strong> na úrovni organizace (ne labels, ne custom fields). Spravují se přes <code>edpa_issue_types.py setup</code>. Filtrování: <code>type:Epic</code>, <code>type:Story</code> atd.</p>
    <div class="card">
      <table>
        <thead>
          <tr><th>Issue Type</th><th>Popis</th></tr>
        </thead>
        <tbody>
          <tr><td>Initiative</td><td>Business case, investiční záměr</td></tr>
          <tr><td>Epic</td><td>Strategický cíl, 6-9 měsíců</td></tr>
          <tr><td>Feature</td><td>Musí se vejít do Planning Intervalu</td></tr>
          <tr><td>Story</td><td>Dodáváno v iteraci</td></tr>
          <tr><td>Defect</td><td>Defekt v existující funkcionalitě</td></tr>
          <tr><td>Task</td><td>Technická práce</td></tr>
        </tbody>
      </table>
      <div class="note"><strong>Enabler</strong> je label klasifikace (Business vs Enabler), ne Issue Type. Epic může mít label &bdquo;Enabler&ldquo; pro označení Enabler Epicu (SAFe).</div>
    </div>
    <div class="card" style="margin-top:16px">
      <table>
        <thead>
          <tr><th>Custom field</th><th>Typ</th><th>Hodnoty</th></tr>
        </thead>
        <tbody>
          <tr><td>Job Size</td><td>Number</td><td>Fibonacci 1&ndash;20</td></tr>
          <tr><td>BV / TC / RR</td><td>Number</td><td>Fibonacci 1&ndash;20</td></tr>
          <tr><td>WSJF Score</td><td>Number</td><td>Auto (Action)</td></tr>
          <tr><td>Planning Interval</td><td>Iteration</td><td>5 nebo 10 týdnů</td></tr>
          <tr><td>Iteration</td><td>Iteration</td><td>1 nebo 2 týdny</td></tr>
          <tr><td>Team</td><td>Single select</td><td>Týmové hodnoty</td></tr>
          <tr><td>Primary Owner</td><td>Assignee</td><td>Accountable owner</td></tr>
        </tbody>
      </table>
      <div class="note">Co <strong>nedržet</strong> jako GitHub field: Capacity, Derived Hours, FTE, podpisový stav &rarr; patří do evidence vrstvy.</div>
    </div>

    <h3>9.2 GitHub Actions</h3>
    <div class="card">
      <table>
        <thead>
          <tr><th>#</th><th>Action</th><th>Trigger</th><th>Funkce</th></tr>
        </thead>
        <tbody>
          <tr><td>1</td><td><strong>WSJF Calculator</strong></td><td>Field change</td><td>Auto-výpočet WSJF</td></tr>
          <tr><td>2</td><td><strong>Contributor Detector</strong></td><td>PR merge / review</td><td>Detekce kontributorů + evidence</td></tr>
          <tr><td>3</td><td><strong>Iteration Close</strong></td><td>Manuální dispatch</td><td>Snapshot + výkazy (MD/JSON/XLSX) + per-item</td></tr>
          <tr><td>4</td><td><strong>PI Close</strong></td><td>Manuální dispatch</td><td>Agregace iterací</td></tr>
          <tr><td>5</td><td><strong>Velocity Tracker</strong></td><td>Iter/PI close</td><td>Velocity JSON + dashboard</td></tr>
        </tbody>
      </table>
    </div>

    <h3>9.3 Branch naming &amp; DoR</h3>
    <div class="formula" style="white-space:pre">feature/S-200-omop-parser
defect/D-215-upload-validation
feature/F-102-anon-engine</div>
    <p>CI check blokuje PR bez reference na issue (<code>S-XXX</code>, <code>F-XXX</code>, <code>E-XXX</code>). DoR: Issue Type, Parent, Job Size, BV+TC+RR, Owner.</p>
  </section>

  <!-- 10. Vykazy a audit -->
  <section id="vykazy">
    <h2>10. Výkazy a audit</h2>

    <h3>10.1 Pipeline</h3>
    <div class="formula" style="white-space:pre">/.edpa/snapshots/
  iteration-PI-2026-1.3.json          &larr; frozen snapshot

/.edpa/reports/
  iteration-PI-2026-1.3/
    vykaz-urbanek.md                   &larr; čitelný výkaz
    vykaz-urbanek.json                 &larr; strojová data
    summary.xlsx                       &larr; souhrnný Excel
    item-costs.xlsx                    &larr; per-item pohled

/signed/
  PI-2026-1.3-urbanek.pdf             &larr; BankID podepsaný</div>

    <h3>10.2 Freeze rule</h3>
    <p>Po Iteration Close: snapshot je <strong>frozen</strong>. Evidence se nepřepisuje in-place. Každá oprava je nová revize. Zásadní pro auditní obhajobu.</p>

    <h3>10.3 Auditni princip</h3>
    <p>Průkaznost stojí na 5 pilírich:</p>
    <ol class="audit-pillars">
      <li>GitHub delivery evidence</li>
      <li>Capacity registry (YAML)</li>
      <li>Frozen snapshot (reprodukovatelný vstup)</li>
      <li>Reprodukovatelný výpočet (Score = JS &times; CW &times; RS)</li>
      <li>Podepsaný výstup (BankID, zákon 21/2020 Sb.)</li>
    </ol>
  </section>

  <!-- 11. Předpoklady a rizika -->
  <section id="predpoklady">
    <h2>11. Předpoklady a rizika</h2>
    <div class="grid grid-2">
      <div class="card green">
        <div class="tag" style="color:var(--gn)">Předpoklady</div>
        <ul>
          <li>Všechny items se uzavírají (nedodané se přesouvají)</li>
          <li>Kapacita potvrzena při Iteration Planning</li>
          <li>Branch naming dodržen (CI check)</li>
          <li>Job Size konzistentní per úroveň</li>
          <li>CW se kalibruje po prvních iteracích</li>
        </ul>
      </div>
      <div class="card">
        <div class="tag" style="color:var(--rd)">Rizika</div>
        <table>
          <thead>
            <tr><th>Riziko</th><th>Mitigace</th></tr>
          </thead>
          <tbody>
            <tr><td>Auditor neuzná</td><td>Metodika + snapshoty + BankID</td></tr>
            <tr><td>CW neodpovida</td><td>Override + kalibrace</td></tr>
            <tr><td>Commit bez S-/F-/E-XXX</td><td>CI check blokuje PR</td></tr>
            <tr><td>PM/Arch bez commitu</td><td>Comments + /contribute</td></tr>
            <tr><td>0 items pro osobu</td><td>Procesní eskalace</td></tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- 12. Srovnání -->
  <section id="srovnani">
    <h2>12. Srovnání s alternativami</h2>

    <div class="grid grid-3" style="margin-bottom:20px">
      <div class="card" style="border-left:3px solid var(--or)">
        <h3 style="color:var(--or);margin-bottom:8px">Fixed Split</h3>
        <p style="font-size:14px;color:var(--t2)">Předem definované koše (např. 60% Dev, 20% Arch, 20% QA). Hodiny se rozdělují fixně podle role, nezávisle na skutečném příspěvku. Jednoduché, ale nepřesné.</p>
      </div>
      <div class="card" style="border-left:3px solid var(--gn)">
        <h3 style="color:var(--gn);margin-bottom:8px">EDPA v{VERSION}</h3>
        <p style="font-size:14px;color:var(--t2)">Evidence-Driven Proportional Allocation. Hodiny se odvozují automaticky z GitHub delivery evidence (commity, PR, reviews). Matematická garance: &Sigma; = Capacity.</p>
      </div>
      <div class="card" style="border-left:3px solid var(--rd)">
        <h3 style="color:var(--rd);margin-bottom:8px">Ruční timesheets</h3>
        <p style="font-size:14px;color:var(--t2)">Každý člen týmu ručně vyplňuje hodiny. Subjektivní, administrativně náročné, nemá per-item pohled, neauditovatelné bez doplňkové evidence.</p>
      </div>
    </div>

    <div class="card">
      <table>
        <thead>
          <tr><th>Vlastnost</th><th style="color:var(--or)">Fixed Split</th><th style="color:var(--gn)">EDPA v{VERSION}</th><th style="color:var(--rd)">Ruční timesheets</th></tr>
        </thead>
        <tbody>
          <tr><td>Fixované koše</td><td>Ano</td><td style="color:var(--gn)">Ne</td><td>Ne</td></tr>
          <tr><td>Prázdné úrovně</td><td style="color:var(--rd)">Problém</td><td style="color:var(--gn)">Neexistují</td><td>N/A</td></tr>
          <tr><td>Per-person pohled</td><td>Ano</td><td style="color:var(--gn)">Ano (primární)</td><td>Ano</td></tr>
          <tr><td>Per-item pohled</td><td style="color:var(--rd)">Ne</td><td style="color:var(--gn)">Ano (dual-view)</td><td>Ne</td></tr>
          <tr><td>Cross-funkční</td><td>Omezená</td><td style="color:var(--gn)">Plná</td><td>Plná</td></tr>
          <tr><td>Automatizace</td><td>Střední</td><td style="color:var(--gn)">Vysoká</td><td style="color:var(--rd)">Žádná</td></tr>
          <tr><td>Mat. garance</td><td>Složitější</td><td style="color:var(--gn)">Nativně</td><td style="color:var(--rd)">Ne</td></tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- 13. Implementační plán -->
  <section id="implementacni-plan">
    <h2>13. Implementační plán</h2>
    <div class="card">
      <table>
        <thead>
          <tr><th>Fáze</th><th>Čas</th><th>Obsah</th></tr>
        </thead>
        <tbody>
          <tr><td style="color:var(--gn)"><strong>Den 1</strong></td><td>6h</td><td>GitHub org, Projects setup, custom fields</td></tr>
          <tr><td style="color:var(--cy)"><strong>Tyden 1</strong></td><td>3 dny</td><td>Actions 1&ndash;2 (WSJF + Contributor Detector)</td></tr>
          <tr><td style="color:var(--ac2)"><strong>Tyden 2</strong></td><td>2 dny</td><td>Actions 3&ndash;5 (Iteration Close + PI Close + Velocity)</td></tr>
          <tr><td style="color:var(--yl)"><strong>Iterace 1</strong></td><td>1&ndash;2t</td><td>Pilotní provoz, první výkazy, kalibrace CW</td></tr>
          <tr><td style="color:var(--or)"><strong>Retro PI 1</strong></td><td>1 den</td><td>Kadence, CW accuracy, velocity, dual-view validace</td></tr>
        </tbody>
      </table>
    </div>
  </section>

  </div><!-- /methodology-content -->
  </div><!-- /methodology-layout -->
</div>

<script>
  // Scroll-spy: highlight active TOC link based on scroll position
  const links = document.querySelectorAll('.toc-link');
  const sections = document.querySelectorAll('section[id]');

  function updateActive() {
    let current = '';
    for (const section of sections) {
      const top = section.getBoundingClientRect().top;
      if (top <= 100) current = section.id;
    }
    links.forEach(link => {
      link.classList.toggle('active', link.dataset.section === current);
    });
  }

  window.addEventListener('scroll', updateActive, { passive: true });
  updateActive();
</script>
</Layout>

<style>
  .methodology-page {
    padding-bottom: 40px;
  }
  .page-header {
    padding: 48px 0 32px;
    border-bottom: 1px solid var(--bd);
    margin-bottom: 40px;
  }
  .page-header h1 {
    font-family: 'JetBrains Mono', monospace;
    font-size: 24px;
    font-weight: 700;
    background: linear-gradient(135deg, var(--ac), var(--cy));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-top: 8px;
  }
  .subtitle {
    font-size: 14px;
    color: var(--t2);
    font-style: italic;
    margin-top: 4px;
  }
  .meta {
    font-size: 11px;
    color: var(--t3);
    margin-top: 6px;
  }

  .methodology-layout {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: 40px;
    align-items: start;
  }
  .sidebar-toc {
    position: sticky;
    top: 72px;
    max-height: calc(100vh - 88px);
    overflow-y: auto;
    background: var(--s1);
    border: 1px solid var(--bd);
    border-radius: var(--r);
    padding: 20px 16px;
  }
  .toc-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: var(--t3);
    margin-bottom: 14px;
  }
  .toc-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .toc-list li {
    margin-bottom: 0;
  }
  .toc-link {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    color: var(--t3);
    padding: 5px 10px;
    border-left: 2px solid transparent;
    border-radius: 0 4px 4px 0;
    transition: all 0.15s;
    text-decoration: none;
    line-height: 1.4;
  }
  .toc-link:hover {
    color: var(--t1);
    background: var(--s2);
  }
  .toc-link.active {
    color: var(--ac2);
    border-left-color: var(--ac);
    background: rgba(99, 102, 241, 0.06);
  }
  .methodology-content {
    min-width: 0;
  }

  section {
    margin-bottom: 48px;
    scroll-margin-top: 72px;
  }
  h2 {
    font-family: 'JetBrains Mono', monospace;
    font-size: 18px;
    font-weight: 700;
    color: var(--t1);
    margin-bottom: 16px;
    padding-bottom: 8px;
    border-bottom: 1px solid var(--bd);
  }
  h3 {
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    font-weight: 600;
    color: var(--ac3);
    margin: 24px 0 12px;
  }
  p {
    font-size: 14px;
    color: var(--t2);
    margin-bottom: 8px;
    line-height: 1.7;
  }
  ul, ol {
    font-size: 14px;
    color: var(--t2);
    padding-left: 20px;
    margin: 8px 0;
  }
  li {
    margin-bottom: 4px;
    line-height: 1.6;
  }

  .layer-operational { color: var(--ac2); }
  .layer-capacity { color: var(--cy); }
  .layer-evidence { color: var(--gn); }

  .audit-pillars {
    padding-left: 24px;
  }
  .audit-pillars li {
    margin-bottom: 6px;
  }

  @media (max-width: 900px) {
    .methodology-layout {
      grid-template-columns: 1fr;
    }
    .sidebar-toc {
      position: static;
      max-height: none;
    }
    .page-header h1 {
      font-size: 18px;
    }
  }
</style>
