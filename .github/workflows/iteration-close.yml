name: EDPA Iteration Close

on:
  workflow_dispatch:
    inputs:
      iteration_id:
        description: 'Iteration ID (e.g., PI-2026-1.3)'
        required: true
        type: string
      mode:
        description: 'Calculation mode'
        required: false
        default: 'simple'
        type: choice
        options:
          - simple
          - full

jobs:
  close-iteration:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: read
      pull-requests: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install pyyaml openpyxl

      - name: Verify configuration
        run: |
          if [ ! -f config/capacity.yaml ]; then
            echo "ERROR: config/capacity.yaml not found. Run EDPA setup first."
            exit 1
          fi
          if [ ! -f config/cw_heuristics.yaml ]; then
            echo "ERROR: config/cw_heuristics.yaml not found. Run EDPA setup first."
            exit 1
          fi
          echo "Configuration verified."

      - name: Run EDPA Engine
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ITERATION_ID: ${{ inputs.iteration_id }}
          MODE: ${{ inputs.mode }}
        run: |
          python scripts/edpa_engine.py \
            --iteration "$ITERATION_ID" \
            --mode "$MODE" \
            --capacity config/capacity.yaml \
            --heuristics config/cw_heuristics.yaml

      - name: Verify invariants
        run: |
          RESULTS="reports/iteration-${{ inputs.iteration_id }}/edpa_results.json"
          if [ ! -f "$RESULTS" ]; then
            echo "ERROR: Engine did not produce results."
            exit 1
          fi
          python -c "
          import json, sys
          with open('$RESULTS') as f:
              data = json.load(f)
          if not data.get('all_invariants_passed'):
              print('INVARIANT FAILURE — check results')
              sys.exit(1)
          print('All invariants passed.')
          "

      - name: Commit results
        run: |
          git config user.name "EDPA Bot"
          git config user.email "edpa@github-actions"
          git add reports/ snapshots/
          git commit -m "feat(edpa): close iteration ${{ inputs.iteration_id }}" || echo "No changes to commit"
          git push
