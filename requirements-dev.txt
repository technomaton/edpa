-r requirements.txt
# Compatible-range pins (S-249): CI installs whatever PyPI serves that day,
# so an unconstrained breaking major release would brick every open PR at
# once. Raise a cap deliberately, in a PR that actually runs on the new major.
pytest>=8,<10
pytest-cov>=5,<8
jsonschema>=4,<5
openpyxl>=3.1,<4
hypothesis>=6,<7
